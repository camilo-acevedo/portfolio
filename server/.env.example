# Database
DATABASE_URL="postgresql://portfolio:portfolio@localhost:5432/portfolio?schema=public"

# Server
PORT=3001
NODE_ENV=production

# CORS — comma-separated list of allowed origins in production
CORS_ORIGIN=https://camilo.dev,https://www.camilo.dev

# SMTP — outbound mail for new contact notifications
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=your_smtp_password_here

# Email addresses
MAIL_FROM="Camilo Portfolio <noreply@camilo.dev>"
MAIL_TO="acevedo8022013@gmail.com"
